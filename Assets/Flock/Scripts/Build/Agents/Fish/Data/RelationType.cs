namespace Flock.Scripts.Build.Agents.Fish.Data {

    /**
     * <summary>
     * Defines the relationship between two fish types in the interaction matrix.
     * </summary>
     */
    public enum RelationType {
        Neutral = 0,
        Friendly = 1,
        Avoid = 2,
    }
}
